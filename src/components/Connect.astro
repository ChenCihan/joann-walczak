---
interface SocialLink {
  label: string;
  href: string;
  icon: 'blog' | 'goodreads';
}

const socialLinks: SocialLink[] = [
  { label: 'Blog — On Layton', href: 'https://joannjoneswalczak.blogspot.com/', icon: 'blog' },
  { label: 'The Walk on Layton (Amazon)', href: 'https://www.amazon.com/Walk-Layton-Modern-Parables-Journey/dp/B0DPTL67J2', icon: 'goodreads' },
];

const iconPaths: Record<SocialLink['icon'], string> = {
  blog: 'M19 5v14H5V5h14m0-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-4.86 8.86l-3 3.87L9 13.14 6 17h12l-3.86-5.14z',
  goodreads: 'M4 19.5A2.5 2.5 0 016.5 17H20V7H6.5A2.5 2.5 0 004 9.5v10zM6.5 5H20v2H6.5A2.5 2.5 0 004 9.5v1A2.5 2.5 0 006.5 5z',
};
---
<section id="connect" class="py-16 md:py-24 bg-white/50 scroll-mt-20 animate-fade-in opacity-0" style="animation-delay: 0.4s">
  <div class="max-w-content mx-auto px-6">
    <h2 class="font-display text-2xl md:text-3xl font-semibold text-navy mb-8">Connect</h2>
    <p class="text-charcoal/80 text-lg max-w-prose mb-10">
      Find me on social media, subscribe to my newsletter, or say hello.
    </p>
    <ul class="flex flex-wrap gap-4">
      {socialLinks.map((item) => (
        <li>
          <a href={item.href} target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-3 px-4 py-3 rounded-lg border border-charcoal/20 text-charcoal hover:border-accent hover:text-accent transition-colors">
            <svg class="w-5 h-5 shrink-0 text-current" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path d={iconPaths[item.icon]} />
            </svg>
            {item.label}
          </a>
        </li>
      ))}
    </ul>
  </div>
</section>
