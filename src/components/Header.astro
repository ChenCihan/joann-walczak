---
const navLinks = [
  { label: 'About', href: '#about' },
  { label: 'Books', href: '#books' },
  { label: 'Blog', href: '#blog' },
  { label: 'Connect', href: '#connect' },
];
---
<header class="sticky top-0 z-50 bg-ink/90 backdrop-blur-md border-b border-white/10 transition-colors duration-350">
  <nav class="max-w-content mx-auto px-6 py-4 flex flex-col sm:flex-row items-center justify-between gap-4 sm:gap-8" aria-label="Main navigation">
    <a href="#" class="font-display text-xl font-semibold text-paper hover:text-accent transition-colors duration-350">Jo Ann Walczak</a>
    <ul class="flex items-center gap-6 sm:gap-8">
      {navLinks.map((link) => (
        <li>
          <a href={link.href} class="text-paper/80 hover:text-accent transition-colors duration-350 text-sm font-medium">{link.label}</a>
        </li>
      ))}
    </ul>
  </nav>
</header>
